<Window x:Class="WpfApp1.AfisareEvenimente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lista Evenimente" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="lstEvenimente" Grid.Row="0" Margin="0,0,0,10"/>

        <Button Content="Închide" Grid.Row="1" Width="100" Click="Button_Click"/>
    </Grid>
</Window>